\documentclass{article}

% The preamble contains instructions affecting the entire document.
% The only required command in the preamble is the \documentclass command.
% There are other commands (such as the \usepackage commands) that must be placed in the preamble if they are used, but such commands do not have to be present in every document.

\usepackage{graphicx}
\newcommand{\pdelta}{\pmod{\delta}}

\begin{document}

$\pdelta$

\begin{figure}[hbt]
    \begin{center}
        \includegraphics{covers} % {\centering}
    \end{center}
    \caption{Theorem illustrated}\label{F:Theorem}
\end{figure}

\end{document}
